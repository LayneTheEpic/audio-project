export {};
/* compacted but unreadable garbage
export type InputDataset = ({
    type: "string";
} | {
    type: "number";
    min: string;
    max: string;
}) & {
    label: string;
    for: string;
    unit?: string;
};
*/
